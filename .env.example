# AdminPC-Server 환경변수 설정
# 이 파일을 복사해서 .env 파일로 사용하세요
# cp .env.example .env

# ── 데이터베이스 ──────────────────────────────
DB_NAME=inspection_logs.db
USERS_DB_NAME=users.db

# ── JWT 설정 ──────────────────────────────────
# 운영 환경에서는 반드시 복잡한 값으로 변경하세요
JWT_SECRET=capstone-vision-mate-2026
JWT_EXPIRY_HOURS=24
